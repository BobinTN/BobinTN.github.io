import {
  l
} from "./chunk-SBW6NNVI.js";

// node_modules/@arcgis/core/core/MemCache.js
var e = -3;
var s;
!function(t) {
  t[t.ALL = 0] = "ALL", t[t.SOME = 1] = "SOME";
}(s || (s = {}));
var i = class {
  constructor(t, e2, s2) {
    this.name = t, this._storage = e2, this.id = h++ + ":", this.size = 0, this.maxSize = 0, this._removeFunc = false, this._hit = 0, this._miss = 0, this._storage.register(this), s2 && (this._storage.registerRemoveFunc(this.id, s2), this._removeFunc = true);
  }
  destroy() {
    this._storage.clear(this), this._removeFunc && this._storage.deregisterRemoveFunc(this.id), this._storage.deregister(this), this._storage = null;
  }
  get hitRate() {
    return this._hit / (this._hit + this._miss);
  }
  get sizeAll() {
    return this._storage.size;
  }
  resetHitRate() {
    this._hit = this._miss = 0;
  }
  put(t, e2, s2, i2 = 0) {
    this._storage.put(this, t, e2, s2, i2);
  }
  get(t) {
    const e2 = this._storage.get(this, t);
    return void 0 === e2 ? ++this._miss : ++this._hit, e2;
  }
  peek(t) {
    return this._storage.peek(this, t);
  }
  pop(t) {
    const e2 = this._storage.pop(this, t);
    return void 0 === e2 ? ++this._miss : ++this._hit, e2;
  }
  updateSize(t, e2, s2) {
    this._storage.updateSize(this, t, e2, s2);
  }
  clear() {
    this._storage.clear(this);
  }
  clearAll() {
    this._storage.clearAll();
  }
  get performanceInfo() {
    return this._storage.performanceInfo;
  }
  resetStats() {
    this._storage.resetStats();
  }
};
var r = class {
  get size() {
    return this._size;
  }
  constructor(e2 = 10485760) {
    this._maxSize = e2, this._db = /* @__PURE__ */ new Map(), this._size = 0, this._hit = 0, this._miss = 0, this._removeFuncs = new l(), this._users = new l();
  }
  destroy() {
    this.clearAll(), this._removeFuncs.clear(), this._users.clear(), this._db = null;
  }
  register(t) {
    this._users.push(t);
  }
  deregister(t) {
    this._users.removeUnordered(t);
  }
  registerRemoveFunc(t, e2) {
    this._removeFuncs.push([t, e2]);
  }
  deregisterRemoveFunc(t) {
    this._removeFuncs.filterInPlace((e2) => e2[0] !== t);
  }
  get maxSize() {
    return this._maxSize;
  }
  set maxSize(t) {
    this._maxSize = Math.max(t, 0), this._checkSizeLimits();
  }
  put(t, i2, r2, h2, o) {
    i2 = t.id + i2;
    const n = this._db.get(i2);
    if (n && (this._size -= n.size, t.size -= n.size, this._db.delete(i2), n.entry !== r2 && this._notifyRemove(i2, n.entry, s.ALL)), h2 > this._maxSize)
      return void this._notifyRemove(i2, r2, s.ALL);
    if (void 0 === r2)
      return void console.warn("Refusing to cache undefined entry ");
    if (!h2 || h2 < 0)
      return console.warn(`Refusing to cache entry with size ${h2} for key ${i2}`), void this._notifyRemove(i2, r2, s.ALL);
    const _ = 1 + Math.max(o, e) - e;
    this._db.set(i2, { entry: r2, size: h2, lifetime: _, lives: _ }), this._size += h2, t.size += h2, this._checkSizeLimits();
  }
  updateSize(t, e2, i2, r2) {
    e2 = t.id + e2;
    const h2 = this._db.get(e2);
    if (h2 && h2.entry === i2) {
      for (this._size -= h2.size, t.size -= h2.size; r2 > this._maxSize; ) {
        const t2 = this._notifyRemove(e2, i2, s.SOME);
        if (!(null != t2 && t2 > 0))
          return void this._db.delete(e2);
        r2 = t2;
      }
      h2.size = r2, this._size += r2, t.size += r2, this._checkSizeLimits();
    }
  }
  pop(t, e2) {
    e2 = t.id + e2;
    const s2 = this._db.get(e2);
    if (s2)
      return this._size -= s2.size, t.size -= s2.size, this._db.delete(e2), ++this._hit, s2.entry;
    ++this._miss;
  }
  get(t, e2) {
    e2 = t.id + e2;
    const s2 = this._db.get(e2);
    if (void 0 !== s2)
      return this._db.delete(e2), s2.lives = s2.lifetime, this._db.set(e2, s2), ++this._hit, s2.entry;
    ++this._miss;
  }
  peek(t, e2) {
    const s2 = this._db.get(t.id + e2);
    return s2 ? ++this._hit : ++this._miss, s2 == null ? void 0 : s2.entry;
  }
  get performanceInfo() {
    const t = { Size: Math.round(this._size / 1048576) + "/" + Math.round(this._maxSize / 1048576) + "MB", "Hit rate": Math.round(100 * this._getHitRate()) + "%", Entries: this._db.size.toString() }, s2 = {}, i2 = new Array();
    this._db.forEach((t2, e2) => {
      const r3 = t2.lifetime;
      i2[r3] = (i2[r3] || 0) + t2.size, this._users.forAll((i3) => {
        const { id: r4, name: h3 } = i3;
        if (e2.startsWith(r4)) {
          const e3 = s2[h3] || 0;
          s2[h3] = e3 + t2.size;
        }
      });
    });
    const r2 = {};
    this._users.forAll((t2) => {
      const e2 = t2.name;
      if ("hitRate" in t2 && "number" == typeof t2.hitRate && !isNaN(t2.hitRate) && t2.hitRate > 0) {
        const i3 = s2[e2] || 0;
        s2[e2] = i3, r2[e2] = Math.round(100 * t2.hitRate) + "%";
      } else
        r2[e2] = "0%";
    });
    const h2 = Object.keys(s2);
    h2.sort((t2, e2) => s2[e2] - s2[t2]), h2.forEach((e2) => t[e2] = Math.round(s2[e2] / 2 ** 20) + "MB / " + r2[e2]);
    for (let o = i2.length - 1; o >= 0; --o) {
      const s3 = i2[o];
      s3 && (t["Priority " + (o + e - 1)] = Math.round(s3 / this._size * 100) + "%");
    }
    return t;
  }
  resetStats() {
    this._hit = this._miss = 0, this._users.forAll((t) => t.resetHitRate());
  }
  clear(t) {
    const e2 = t.id;
    this._db.forEach((t2, i2) => {
      i2.startsWith(e2) && (this._size -= t2.size, this._db.delete(i2), this._notifyRemove(i2, t2.entry, s.ALL));
    }), t.size = 0;
  }
  clearAll() {
    this._db.forEach((t, e2) => this._notifyRemove(e2, t.entry, s.ALL)), this._users.forEach((t) => t.size = 0), this._size = 0, this._db.clear();
  }
  _getHitRate() {
    return this._hit / (this._hit + this._miss);
  }
  _notifyRemove(t, e2, s2) {
    let i2;
    return this._removeFuncs.some((r2) => {
      if (t.startsWith(r2[0])) {
        const t2 = r2[1](e2, s2);
        return "number" == typeof t2 && (i2 = t2), true;
      }
      return false;
    }), i2;
  }
  _checkSizeLimits() {
    if (this._size > this._maxSize) {
      for (const [t, e2] of this._db)
        if (this._purgeItem(t, e2), this._size <= 0.9 * this.maxSize)
          return;
    }
    this._users.forEach((t) => {
      if (t.maxSize > 0 && t.size > t.maxSize) {
        for (const [e2, s2] of this._db)
          if (e2.startsWith(t.id) && (this._purgeItem(e2, s2, t), t.size <= 0.9 * t.maxSize))
            return;
      }
    });
  }
  _purgeItem(t, e2, i2 = this._users.find((e3) => t.startsWith(e3.id))) {
    if (this._db.delete(t), e2.lives <= 1) {
      this._size -= e2.size, i2 && (i2.size -= e2.size);
      const r2 = this._notifyRemove(t, e2.entry, s.SOME);
      null != r2 && r2 > 0 && (this._size += r2, i2 && (i2.size += r2), e2.lives = e2.lifetime, e2.size = r2, this._db.set(t, e2));
    } else
      --e2.lives, this._db.set(t, e2);
  }
};
var h = 0;

export {
  i,
  r
};
//# sourceMappingURL=chunk-KWSMYM7P.js.map
